name: Deploy to Github Pages

on:
  push:
    branches:
      - main

  workflow_dispatch:

# Required for peaceiris/actions-gh-pages when using GITHUB_TOKEN (push to gh-pages branch).
permissions:
  contents: write

jobs:
  deployment:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: ['25.8.2']

    steps:
      - name: Checking out the latest code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setting up nodejs for building codebase and Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Verify pnpm
        run: pnpm -v

      - name: Installing Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install jq for cache scripts
        run: sudo apt-get update && sudo apt-get install -y jq

      # Order matters: story-pages index needs posts + categories + image URL map.
      # Without running image URLs before build-story-pages-index, the index can be empty.

      - name: Fetch posts
        env:
          POST_FETCH_PAGES: ${{ vars.POST_FETCH_PAGES }}
        run: |
          # Skip heavy WP pagination when unset and committed snapshot exists (default for CI speed).
          # Set repo Actions variable POST_FETCH_PAGES to refresh (e.g. small number for smoke tests).
          if [ -z "${POST_FETCH_PAGES:-}" ] && [ -f "src/assets/cached/posts.json" ]; then
            echo "Skipping fetch-the-standard-posts.sh — using committed posts.json"
          else
            bash cachescripts/fetch-the-standard-posts.sh
          fi

      - name: Fetch categories
        run: bash cachescripts/fetch-the-standard-categories.sh

      - name: Fetch image URLs
        env:
          # Keep CDN URLs in JSON only — do not download binaries into the repo (faster CI).
          # Browser <img src="https://..."> is not subject to CORS like fetch() is.
          LOCALIZE_IMAGE_ASSETS: '0'
        run: bash cachescripts/fetch-image-urls.sh

      - name: Build story pages index
        run: bash cachescripts/build-story-pages-index.sh

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Add .nojekyll file
        run: touch ./dist/.nojekyll

      # GITHUB_TOKEN + contents:write replaces a deploy SSH key for same-repo Pages.
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
